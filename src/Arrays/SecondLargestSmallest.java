package Arrays;

public class SecondLargestSmallest {
    /*public static void main(String[] args) {
        findLargestAndSmallest(new int[]{-110,1, 2, 3, 4, 6, 9, 8,1000,5});

    }

    private static void findLargestAndSmallest(int[] numbers)
    {

         int i, first, second = Integer.MIN_VALUE;
       if (numbers.length < 2)
              {
                  int secondLargest = 0;
                  System.out.print(" Invalid Input ");  // for(int i =1; i<numbers.length;i++)
                  return;
              }

           *//*   if(numbers[i]>first)
           {
               max = numbers[i-1];
               secondLargest = numbers[i-1];
           }
            if(numbers[i]<min)
            {
                min =numbers[i];
            }
        }
        System.out.println("max "+max);
        System.out.println(" Second largest max "+secondLargest);
    }

    private static void findLargestAndSmallest1(int[] numbers) {

        
    }*//*
*/
}
